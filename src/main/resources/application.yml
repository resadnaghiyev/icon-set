server:
  port: 8181

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/test
    username: rashad
    password:
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
#        format_sql: true
    show-sql: true
